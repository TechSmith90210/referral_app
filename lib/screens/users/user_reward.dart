import 'package:flutter/material.dart';

class UserReward extends StatelessWidget {
  const UserReward({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("User Reward");
  }
}
